{
  val nameR4 = SELF.R4[Coll[Byte]].get
  val tldR5 = SELF.R5[Coll[Byte]].get
  // R6 = resolved-to-address, is allowed to change

  // ensure nft is preserved
  // successor prop bytes unchanged

  // allow transferring to someone else with name/tld preserved
}